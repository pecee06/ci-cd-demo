version: "3.8"

services:
  web:
    # Replace with your Docker Hub username and repository name
    image: priyanshu2406/ci-cd-demo:latest
    env_file:
      - .env
    ports:
      - "80:3000"
    restart: always
